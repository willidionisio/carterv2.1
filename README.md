# Carter Bot v2.1 🤖

Bot modular para Discord focado em **gestão de comunidade/família** (RP), com **Slash Commands**, **Interações (Buttons/Modals)** e **persistência com SQLite**.

Construído para ser simples de manter, fácil de evoluir e seguro no uso do dia a dia (com controle de permissão por cargo em comandos administrativos).

---

## ✨ Destaques

- ✅ Slash Commands (discord.js)
- ✅ Interações avançadas: **buttons** + **modals**
- ✅ Sistema de permissões por cargo (ex.: `/anuncio` apenas para cargos autorizados)
- ✅ SQLite para persistência
- ✅ Estrutura modular (commands / interactions / utils / database)
- ✅ Pronto para escalar (novos comandos e módulos sem bagunça)

---

## 🧱 Estrutura do Projeto


---

## 🔐 Segurança (IMPORTANTE)

Não suba seu `config.json` com token no repositório.

Use `.env` (recomendado) ou mantenha `config.json` fora do Git com `.gitignore`.

---

## 🚀 Setup rápido

1) Instale dependências:
```bash
npm install
